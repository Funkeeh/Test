using UnityEngine;
using System.Collections;
using UnityEngine.UI;
using System;

public class calibVerticalPos : MonoBehaviour
{
    public GameObject rootPos;
    public GameObject rootPosFemale;
    //private float adjustment = 0.0f;

    public void AdjustRootHeight( float number )
    {
        //int useInCase = (int)number;
        //adjustment = number * 0.01f;
        //rootPos.transform.position = new Vector3 (rootPos.transform.position.x, IKavatar.rootYPos + adjustment, rootPos.transform.position.z);
        //rootPosFemale.transform.position = new Vector3(rootPosFemale.transform.position.x, IKavatar.rootYPos + adjustment, rootPosFemale.transform.position.z);
    }
}